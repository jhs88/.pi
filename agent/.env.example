# Required: explicit self-hosted Firecrawl API endpoint.
# Firecrawl Cloud is intentionally not used as a fallback.
FIRECRAWL_API_URL=https://firecrawl.example.internal

# Optional for self-hosted deployments that require authentication.
# FIRECRAWL_API_KEY=
