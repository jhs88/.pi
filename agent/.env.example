# Optional Pi-local override for the global Hermes Firecrawl endpoint.
# An explicit self-hosted URL is still required; Firecrawl Cloud is never a fallback.
FIRECRAWL_API_URL=https://firecrawl.example.internal

# Optional for self-hosted deployments that require authentication.
# FIRECRAWL_API_KEY=
